<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LearningApp"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Activity principale (Login ou Home) -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity Détail du Cours -->
        <activity
            android:name=".activities.CourseDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.CourseDetail"
            android:parentActivityName=".activities.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.MainActivity" />
        </activity>

        <!-- Activity Lecteur Vidéo (à créer plus tard) -->
        <activity
            android:name=".activities.VideoPlayerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.VideoPlayer"
            android:parentActivityName=".activities.CourseDetailActivity" />

        <!-- Activity Visualisation PDF (à créer plus tard) -->
        <activity
            android:name=".activities.PdfViewerActivity"
            android:exported="false"
            android:parentActivityName=".activities.CourseDetailActivity" />

    </application>

</manifest>